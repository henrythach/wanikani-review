import React from 'react';

export const Header = () => (
  <header>
    <nav>
      <button type={'button'}>Critical Items</button>
      <button type={'button'}>Recent Learned</button>
    </nav>
  </header>
);
